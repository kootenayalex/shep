<?xml version="1.0" encoding="utf-8"?>
<!-- Nothing the app stores belongs on another device: the bridge pairing is a
     bearer token for a shell on Alex's machines, and the rest is cache. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" />
        <exclude domain="database" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" />
        <exclude domain="database" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
